<%= simple_form_for(daddy_service) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :price, input_html: { step: :any } %>
  <%= f.association :category, collection: Category.all %>
   <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>
